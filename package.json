{
  "name": "training-stat",
  "version": "1.0.0",
  "description": "Take data from Notion and try to discover my training results",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js",
    "build": "npx @11ty/eleventy --input=. --output=."
  },
  "author": "plotnikov-ep",
  "license": "ISC",
  "dependencies": {
    "@notionhq/client": "^2.2.2",
    "dotenv": "^16.0.3"
  }
}
